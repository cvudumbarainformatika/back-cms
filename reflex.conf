# Reflex configuration for hot reload in development
# Watch all .go files and restart the app on changes

# Pattern: watch .go files, exclude vendor and test files
-r '\.go$' -s -- sh -c 'go run main.go'
